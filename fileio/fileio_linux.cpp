#include "fileio.h"

namespace Dodger {
  namespace FileIO {

    bool Initialize() {
      return true;
    }

    void ReadFile(const char* fileName, uint8_t*  data) {

    }

    bool Shutdown() {
      return true;
    }
  }
}
